apiVersion: v1
kind: Service
metadata:
  name: gateway-core
spec:
  selector:
    app: gateway-core
  ports:
    - name: app
      port: 9000
      targetPort: 9000
    - name: management
      port: 9001
      targetPort: 9001
  type: LoadBalancer
